# ИТ-М — реклама на ТВ (Full HD)

Открытие из **Админ-панель → ТВ-реклама**: «Найти мониторы» → выбрать ТВ → «Открыть на весь экран».

## Как это работает

- CRM запускает **отдельный** Chrome/Edge (`runtime/tv-chrome-profile`, `--app=…`).
- Окно позиционируется по координатам монитора из ОС (`EnumDisplayMonitors`).
- Окно CRM **не** переносится и не закрывается.
- **Esc** на ТВ закрывает только окно рекламы (`POST /tv/close` + `window.close`).

## Файлы

| Файл | Назначение |
|---|---|
| `bg-three-windows-fhd.png` | Фон 1920×1080 |
| `slide-01.png` … `slide-08.png` | Слайды |
| Страница CRM `/tv` | Карусель в киоск-окне |

Страница: `/tv?fs=1&os=1&kiosk=ITM-TV-ADS-KIOSK`
